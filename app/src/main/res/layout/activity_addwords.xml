<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:orientation="vertical"
              android:layout_width="match_parent"
              android:layout_height="match_parent" android:visibility="visible">

    <EditText
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:id="@+id/edtAddText" android:visibility="visible"/>
    <ProgressBar
            style="?android:attr/progressBarStyleSmall"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/progressBarAddWords" android:visibility="gone"
            android:layout_gravity="center_horizontal" android:indeterminate="false"/>
    <Button
            android:layout_width="294dp"
            android:layout_height="wrap_content"
            android:text="Add"
            android:id="@+id/btnAddWord" android:layout_gravity="center_horizontal" android:visibility="visible"/>
    <ListView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/wordList" android:layout_gravity="center_horizontal"/>
</LinearLayout>